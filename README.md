# Ansible Project for Node.js App Deployment

This project contains Ansible playbooks to deploy and manage a Node.js application.

## Prerequisites

- Ansible installed
- Access to the target servers with the SSH key specified in `inventory.ini`.

## Usage

To run the main playbook, execute the following command:

```bash
ansible-playbook site.yml
```
